import { finishThoughtPreviewMode } from '~/pages/HomePage/slice';

const finishThoughtPreviewModeThunk = () => dispatch => dispatch(finishThoughtPreviewMode());

export default finishThoughtPreviewModeThunk;
